#include <stdio.h>

int main()
{
    sum = 0; product = 1;
    n = 0
    scanf("%d", &x);
    while(x != 0){
        sum += x;
        product *= x;
        n ++;
        scanf("%d", &x);
    }
    printf("%d %d %d", n, sum, profuct);
}
